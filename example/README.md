# react-native-nvshortvideo-example

## Getting started

`$ yarn install`

> [!note]  
> If you use the npm command, refer to the [npm documentation](https://docs.npmjs.com/cli/install)

### Manual installation

#### iOS

`cd ios && pod install`

#### Android
`cd android && ./gradlew clean`
